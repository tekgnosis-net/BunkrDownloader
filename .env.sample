# Server configuration
API_HOST=0.0.0.0
API_PORT=8887

# Local path that will be mounted into the container (docker-compose)
DOWNLOADS_DIR=./Downloads

# Frontend development settings
VITE_DEV_SERVER_PORT=5173
VITE_API_PROXY=http://localhost:8887
VITE_WS_BASE_URL=
